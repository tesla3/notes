# pi, tool allowlist (over mcp) for external connectity: list a set of tools (cli) "allow list" in system prompt. and priodically remind it (reinsert the allow list) 

---
# The twin is a different representation of the same system, optimized for a different purpose. Production is optimized for speed, scale, and reliability. The
 │ twin is optimized for correctness, readability, and malleability. These are complementary, not redundant. The twin isn't a copy of production — it's a
 │ projection of production onto the correctness dimension only.

